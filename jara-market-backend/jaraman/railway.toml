[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build && composer install --no-dev --optimize-autoloader --ignore-platform-reqs"

[deploy]
startCommand = "bash start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
